listen: :8000
metrics: true
gzip: true
verbose: false
logging: scarecrow.log

domain: example.com
tls: true
email: john@doe.com

registry:
  - service:
    path: /
    proxy: localhost:9002
  - service:
    path: /v1
    proxy: localhost:9001
